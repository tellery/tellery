export const PopoverMotionVariants = {
  scale: {
    inactive: { opacity: 0, transform: 'scale(0.8)' },
    active: { opacity: 1, transform: 'scale(1)' }
  },
  fade: {
    inactive: { opacity: 0 },
    active: { opacity: 1 }
  }
}
